# Case <codename> — evidence kit

**Frozen on:** <date> — no silent edits after this date. If a fact later proves wrong,
it enters as a dated amendment in an "## Amendments" section at the end (rules.md §4a);
the original text is never rewritten.
Redact names freely. Never redact numbers, dates, or the order of events.

## 1 — The pricing moment

<the thread, proposal section, or call notes where the number was set — including what was
said just before and after. If someone else (or an AI) produced the estimate, include it
and what it was based on, or record that it could not be obtained.>

## 2 — The scope as delivered

<everything actually done, listed plainly, including additions along the way>

<!-- Summarize deliverables and events, never files. A project with two hundred files is
still ten or fifteen delivered things: name each thing, when it was added, and whether it
was in the priced scope. For a large project, have Claude draft this list by walking the
project folder in a SEPARATE session, then correct it by hand and paste only the corrected
list here — the project files themselves never enter the diagnosis. Test per line: does
this line bear on price versus work done? If not, cut it. -->

## 3 — The time spent

<real hours, however roughly logged — an honest estimate beats a tidy invention>

## 4 — The scope-shift record

<what was said (or not said) each time the work grew past the original agreement>

---

After the cold run, the unedited transcript is saved beside this file as `transcript.md`.
